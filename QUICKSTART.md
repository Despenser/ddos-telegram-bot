# Быстрый старт

Это краткое руководство поможет вам быстро начать работу с Telegram Bot Load Tester.

## Шаг 1: Подготовка

1. Убедитесь, что у вас установлена Java 21 или выше:
   ```bash
   java -version
   ```

2. Получите токен вашего Telegram бота от [@BotFather](https://t.me/BotFather)

3. Получите Chat ID:
   - Отправьте сообщение вашему боту
   - Используйте [@userinfobot](https://t.me/userinfobot) для получения вашего Chat ID
   - Или используйте метод `getUpdates` API: `https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates`

## Шаг 2: Настройка

Отредактируйте `src/main/resources/application.yml`:

```yaml
telegram:
  bot:
    url: "https://api.telegram.org/bot<YOUR_BOT_TOKEN>"
```

Замените `<YOUR_BOT_TOKEN>` на ваш реальный токен.

## Шаг 3: Запуск

### Windows:
Дважды кликните на `run.bat` или выполните в командной строке:
```cmd
run.bat
```

### Linux/Mac:
```bash
chmod +x run.sh
./run.sh
```

### Или используйте Maven напрямую:
```bash
mvn spring-boot:run
```

## Шаг 4: Отправка первого теста

Откройте новый терминал и выполните:

```bash
curl -X POST http://localhost:8080/api/load-test/start \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": "YOUR_CHAT_ID",
    "messageText": "Мой первый тест",
    "threadCount": 5,
    "requestsPerThread": 10,
    "delayBetweenRequests": 200
  }'
```

Замените `YOUR_CHAT_ID` на ваш реальный Chat ID.

## Шаг 5: Проверка результатов

Скопируйте `testId` из ответа предыдущего запроса и выполните:

```bash
curl http://localhost:8080/api/load-test/YOUR_TEST_ID
```

Замените `YOUR_TEST_ID` на реальный ID теста.

## Что дальше?

- Изучите [полную документацию](README.md)
- Попробуйте примеры из `examples/requests.http`
- Экспериментируйте с разными параметрами нагрузки

## Частые проблемы

### Получаю ошибку 429 (Too Many Requests)

Это нормально при высокой нагрузке. Telegram API имеет лимиты. Решение:
- Увеличьте `delayBetweenRequests` до 300-500 мс
- Уменьшите `threadCount` до 3-5

### Не получаю ответ от API

Проверьте:
- Правильность токена бота
- Правильность Chat ID
- Доступность интернета
- Логи приложения в консоли

### Приложение не запускается

Проверьте:
- Версию Java (должна быть 21+)
- Наличие Maven
- Правильность конфигурации в `application.yml`

---

**Готово!** Теперь вы можете тестировать стабильность вашего Telegram бота! 🚀

